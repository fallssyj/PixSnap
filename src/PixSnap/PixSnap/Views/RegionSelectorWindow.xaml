<Window
    x:Class="PixSnap.Views.RegionSelectorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Region Selector"
    AllowsTransparency="True"
    Background="Transparent"
    Cursor="Cross"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None">
    <Canvas x:Name="RootCanvas" Background="#01000000">
        <!--  这些命名元素由 code-behind 直接更新，用于高频覆盖层绘制，避免把 Canvas 坐标细节泄漏到 ViewModel。  -->
        <Rectangle
            x:Name="Mask"
            Width="0"
            Height="0"
            Fill="#7F08111F" />
        <Rectangle
            x:Name="WindowHighlightRect"
            Fill="#14F97316"
            Stroke="#FFF97316"
            StrokeThickness="3"
            Visibility="Collapsed" />
        <Rectangle
            x:Name="SelectionRect"
            Fill="#2238BDF8"
            Stroke="#FF38BDF8"
            StrokeThickness="2"
            Visibility="Collapsed" />

        <Border
            x:Name="InfoBubble"
            Padding="12,10"
            Background="#E6111827"
            CornerRadius="14">
            <StackPanel>
                <TextBlock
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="{Binding SelectionText}" />
                <TextBlock
                    MaxWidth="420"
                    Margin="0,6,0,0"
                    FontSize="12"
                    Foreground="#FFD8E3F0"
                    Text="{Binding InstructionText}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border
            Canvas.Right="24"
            Canvas.Bottom="24"
            Padding="14,10"
            Background="#DDEFF6FF"
            CornerRadius="16">
            <TextBlock Foreground="#FF0F172A" Text="左键单击高亮窗口截图，左键拖动截矩形，Space 截当前显示器，Esc / 右键退出" />
        </Border>
    </Canvas>
</Window>