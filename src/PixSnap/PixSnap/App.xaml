<Application
    x:Class="PixSnap.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemeResources />
                <ui:XamlControlsResources />
                <ResourceDictionary Source="Styles/Typography.xaml" />
                <ResourceDictionary Source="Styles/Overlay.xaml" />
                <ResourceDictionary Source="Styles/PreviewResources.xaml" />
                <ResourceDictionary Source="Styles/AboutWindow.xaml" />
                <ResourceDictionary Source="Styles/ToolbarStyles.xaml" />
                <ResourceDictionary Source="Styles/SettingsPageStyles.xaml" />
                <ResourceDictionary Source="Styles/AppStyles.xaml" />
                <ResourceDictionary Source="Styles/AnnotationDockStyles.xaml" />
                <ResourceDictionary Source="Controls/FloatingPanel.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
            </Style>
            <Style BasedOn="{StaticResource AppTextBlockBaseStyle}" TargetType="TextBlock" />
            <Style TargetType="{x:Type ui:FontIcon}">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>
            <Style TargetType="TreeViewItem">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>

            <!--  在 iNKORE Modern 主题之上叠加统一字体与手型光标  -->
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static ui:ThemeKeys.DefaultButtonStyleKey}}">
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>
            <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Static ui:ThemeKeys.DefaultToggleButtonStyleKey}}">
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>
            <Style TargetType="RepeatButton" BasedOn="{StaticResource {x:Static ui:ThemeKeys.DefaultRepeatButtonStyleKey}}">
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static ui:ThemeKeys.DefaultTextBoxStyleKey}}">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>
            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static ui:ThemeKeys.DefaultComboBoxStyleKey}}">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSizeBody}" />
            </Style>

            <tb:TaskbarIcon x:Key="TrayIcon" ToolTipText="PixSnap" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
