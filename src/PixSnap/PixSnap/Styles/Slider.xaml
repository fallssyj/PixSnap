<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="SliderThumbStyle" TargetType="Thumb">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Width" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                        x:Name="ThumbRoot"
                        Background="{DynamicResource SliderThumbBrush}"
                        BorderBrush="{DynamicResource SliderThumbBorderBrush}"
                        BorderThickness="1.5"
                        CornerRadius="9">
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="10"
                                ShadowDepth="0"
                                Color="#66000000" />
                        </Border.Effect>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbRoot" Property="Background" Value="{DynamicResource SliderThumbHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbRoot" Property="Background" Value="{DynamicResource SliderThumbPressedBrush}" />
                            <Setter TargetName="ThumbRoot" Property="Width" Value="15" />
                            <Setter TargetName="ThumbRoot" Property="Height" Value="15" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ThumbRoot" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HorizontalSliderRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="0"
                        CornerRadius="3" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="HorizontalSliderTemplate" TargetType="Slider">
        <Grid Margin="2,8">
            <Border
                Height="6"
                VerticalAlignment="Center"
                Background="{DynamicResource SliderTrackBrush}"
                BorderBrush="{DynamicResource SliderTrackBorderBrush}"
                BorderThickness="1"
                CornerRadius="3" />
            <Track x:Name="PART_Track" VerticalAlignment="Center">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Background="{DynamicResource SliderActiveTrackBrush}" Style="{StaticResource HorizontalSliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Background="Transparent" Style="{StaticResource HorizontalSliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="VerticalSliderTemplate" TargetType="Slider">
        <Grid Margin="8,2">
            <Border
                Width="6"
                HorizontalAlignment="Center"
                Background="{DynamicResource SliderTrackBrush}"
                BorderBrush="{DynamicResource SliderTrackBorderBrush}"
                BorderThickness="1"
                CornerRadius="3" />
            <Track
                x:Name="PART_Track"
                HorizontalAlignment="Center"
                IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Background="{DynamicResource SliderActiveTrackBrush}" Style="{StaticResource HorizontalSliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Background="Transparent" Style="{StaticResource HorizontalSliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="Slider">
        <Setter Property="Background" Value="{DynamicResource SliderTrackBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SliderTrackBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SliderActiveTrackBrush}" />
        <Setter Property="Height" Value="28" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="Template" Value="{StaticResource HorizontalSliderTemplate}" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="28" />
                <Setter Property="MinHeight" Value="120" />
                <Setter Property="Template" Value="{StaticResource VerticalSliderTemplate}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.55" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="PreviewZoomSliderStyle"
        BasedOn="{StaticResource {x:Type Slider}}"
        TargetType="Slider">
        <Setter Property="Width" Value="300" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Background" Value="{DynamicResource PreviewZoomTrackInactiveBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PreviewZoomTrackActiveBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Margin="6,0">
                        <Border
                            Height="8"
                            VerticalAlignment="Center"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Opacity="0.92" />
                        <Track x:Name="PART_Track" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Background="{TemplateBinding Foreground}" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border
                                                Height="8"
                                                VerticalAlignment="Center"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="4" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb
                                    Width="22"
                                    Height="22"
                                    Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Grid>
                                                <Ellipse
                                                    Width="34"
                                                    Height="34"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Fill="#443C4048">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect
                                                            BlurRadius="8"
                                                            ShadowDepth="0"
                                                            Color="#5532363D" />
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                                <Ellipse
                                                    Width="22"
                                                    Height="22"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Fill="{DynamicResource PreviewZoomTrackActiveBrush}"
                                                    Stroke="#FF8FD2FF"
                                                    StrokeThickness="1" />
                                            </Grid>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Background="Transparent" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>