<UserControl
    x:Class="PixSnap.Controls.DockSliderField"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Focusable="False">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Column="0"
            MinWidth="32"
            VerticalAlignment="Center"
            Style="{StaticResource PanelCompactFieldLabelStyle}"
            Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <TextBox
            Grid.Column="1"
            Margin="10,0,0,0"
            VerticalAlignment="Center"
            Style="{StaticResource EditDockInlineNumberBoxStyle}"
            Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0}}" />
        <TextBlock
            Grid.Column="2"
            Margin="6,0,0,0"
            VerticalAlignment="Center"
            Text="{Binding Unit, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock.Style>
                <Style BasedOn="{StaticResource PanelCompactFieldLabelStyle}" TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Unit, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <Slider
            Grid.Column="3"
            Width="104"
            Margin="14,0,0,0"
            VerticalAlignment="Center"
            IsSnapToTickEnabled="True"
            LargeChange="{Binding LargeChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Maximum="{Binding Maximum, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Minimum="{Binding Minimum, RelativeSource={RelativeSource AncestorType=UserControl}}"
            SmallChange="{Binding SmallChange, RelativeSource={RelativeSource AncestorType=UserControl}}"
            TickFrequency="{Binding TickFrequency, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Value="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
    </Grid>
</UserControl>
